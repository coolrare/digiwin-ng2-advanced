import { CounterbtnDirective } from './counterbtn.directive';

describe('CounterbtnDirective', () => {
  it('should create an instance', () => {
    const directive = new CounterbtnDirective();
    expect(directive).toBeTruthy();
  });
});
